{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "outputDirectory": "public",
  "buildCommand": "bun run build:vercel",
  "rewrites": [
    {
      "source": "api/:path*",
      "destination": "/:path*"
    }
  ]
}
